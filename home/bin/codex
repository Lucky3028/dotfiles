#!/bin/sh

set -eu

codex_bin=$(mise which codex)

for arg in "$@"; do
  case "$arg" in
    --profile | -p | --profile=* | -p?*)
      exec "$codex_bin" "$@"
      ;;
  esac
done

exec "$codex_bin" --profile dotfiles "$@"
